version: '3.8'

services:
  web:
    build: .
    environment:
      - SITE_URL=https://isxengines.com
    volumes:
      - uploads:/var/www/html/assets/uploads
    restart: unless-stopped

volumes:
  uploads:
